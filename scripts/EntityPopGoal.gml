var entity = argument0;
/*

ASSERT(entity != -1, "Entity does not exist in EntityPopGoal()");

with(ds_stack_pop(entity.goal_stack)){
    instance_destroy();
    }
    
ASSERT(!ds_stack_empty(entity.goal_stack), EntityGetName(entity)+" goal stack has become empty");
*/
